import 'package:flutter/material.dart';

class CekSaldo extends StatelessWidget {
  const CekSaldo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cek Saldo'),
      ),
    );
  }
}
